# typed-schema

Typed Rust schema index primitives and derive macros for agent-facing capability
contracts.

This repository owns only the business-neutral schema model:

- `typed-schema`: schema index types, validation, and the `index!` macro.
- `typed-schema-macros`: derives for `Shape` and `Op`.

